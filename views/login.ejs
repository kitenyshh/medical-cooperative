<%- include('layout', { 
  body: `
    <div class="login-container">
      <h2>🔐 Авторизация врача</h2>
      ${error ? `<div class="error">${error}</div>` : ''}
      <form method="POST">
        <input name="login" placeholder="Логин" required value="doctor1">
        <input name="password" type="password" placeholder="Пароль" required value="doctor123">
        <button type="submit" class="button-primary">ВОЙТИ</button>
      </form>
      <p>Нет аккаунта? <a href="/register">Зарегистрироваться</a></p>
    </div>
  `,
  doctor: null
}) %>

<%- include('layout', { 
  body: `
    <div class="exam-container">
      <h2>Новый пациент</h2>
      ${error ? `<div class="error">${error}</div>` : ''}
      <form method="POST">
        <input name="name" placeholder="ФИО пациента" required>
        <select name="gender" required>
          <option value="">Пол</option>
          <option value="М">Мужской</option>
          <option value="Ж">Женский</option>
        </select>
        <input name="birth_date" type="date" placeholder="Дата рождения">
        <input name="home_address" placeholder="Домашний адрес">
        <button type="submit">СОХРАНИТЬ ПАЦИЕНТА</button>
      </form>
    </div>
  `,
  doctor 
}) %>

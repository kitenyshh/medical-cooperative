<%- include('layout', { 
  body: `
    <div class="exam-container">
      <h2>🩺 Новый осмотр</h2>
      <form method="POST">
        <h3>👤 Пациент</h3>
        <div class="form-row">
          <select name="patient_id" required>
            <option value="">Выберите пациента</option>
            ${
              patients.map(p => `
                <option value="${p.id}">${p.name} (${p.gender || '-'}, ${p.birth_date || '-'})</option>
              `).join('')
            }
          </select>
        </div>

        <div class="form-row">
          <a href="/patients/new">+ Добавить нового пациента</a>
        </div>

        <h3>📅 Параметры приёма</h3>
        <div class="form-row">
          <input name="visit_date" type="date" required>
          <select name="location" required>
            <option>Приём</option>
            <option>На дому</option>
          </select>
        </div>

        <h3>📝 Жалобы и диагноз</h3>
        <textarea name="symptoms" placeholder="Симптомы" required rows="3"></textarea>

        <div class="form-row">
          <select name="diagnosis_id" required>
            <option value="">Выберите диагноз</option>
            ${
              diagnoses.map(d => `
                <option value="${d.id}">${d.name}</option>
              `).join('')
            }
          </select>
        </div>

        <textarea name="prescription_text" placeholder="Предписания пациенту" required rows="2"></textarea>

        <h3>💊 Назначение лекарства</h3>

        <div class="form-row">
          <select name="existing_medicine_id">
            <option value="">Выбрать существующее лекарство</option>
            ${
              medicines.map(m => `
                <option value="${m.id}">${m.name}</option>
              `).join('')
            }
          </select>
        </div>

        <h3>➕ Или добавить новое лекарство</h3>

        <input name="new_med_name" placeholder="Название лекарства">
        <input name="new_med_intake" placeholder="Способ приема">
        <textarea name="new_med_action" placeholder="Описание предполагаемого действия" rows="2"></textarea>
        <textarea name="new_med_side" placeholder="Возможные побочные эффекты" rows="2"></textarea>

        <button type="submit" class="button-primary">💾 СОХРАНИТЬ ОСМОТР</button>
      </form>
    </div>
  `,
  doctor 
}) %>
